export interface Achievement {
  id: string
  title: string
  date: string
  description: string
}

export interface LearningTrackItem {
  id: string
  title: string
  description: string
  status: 'in-progress'
}
